<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusSaathi">
        <activity
            android:name=".VerificationVerified"
            android:exported="false" />
        <activity
            android:name=".VerificationStatus"
            android:exported="false" />
        <activity
            android:name=".VerificationFailed"
            android:exported="false" />
        <activity
            android:name=".OwnerVerification"
            android:exported="false" />
        <activity
            android:name=".ChooseOwnerType"
            android:exported="false" />

<!--        <activity-->
<!--            android:name=".MainActivity"-->
<!--            android:exported="true">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
<!--        </activity>-->

        <!-- Splash screen (Launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login screen -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <!-- Signup screen -->
        <activity
            android:name=".SignupActivity"
            android:exported="false" />
        <!-- Role selection -->
        <activity
            android:name=".RoleSelectionActivity"
            android:exported="false" />
        <!-- Student dashboard -->
        <activity
            android:name=".StudentDashboardActivity"
            android:exported="false" />
        <!-- Main / Home (optional) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".OwnerDashboardActivity"
            android:exported="false" />


    </application>

</manifest>